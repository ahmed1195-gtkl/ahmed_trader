# إعداد Firebase - نظام الرسائل المبسط

## 📋 نظرة عامة

تم تبسيط نظام الرسائل ليعمل بكفاءة:
- **المستخدمون العاديون**: يراسلون الأدمن فقط
- **الأدمن**: يرى قائمة المستخدمين ويمكنه الرد على رسائلهم

---

## 🔧 خطوات التفعيل

### 1. افتح Firebase Console
```
https://console.firebase.google.com
```

### 2. اختر مشروعك

### 3. اذهب إلى Firestore Database > Rules

### 4. انسخ القواعد من ملف `firestore.rules`

### 5. الصق القواعد واضغط "Publish"

---

## 📊 هيكل البيانات

### مجموعة `messages`

كل رسالة تحتوي على:

```javascript
{
  text: "نص الرسالة",
  userId: "معرف المستخدم صاحب الرسالة",
  userName: "اسم المستخدم",
  userPhoto: "صورة المستخدم",
  senderId: "معرف المرسل الفعلي",
  senderName: "اسم المرسل",
  senderPhoto: "صورة المرسل",
  isAdmin: true/false,
  participants: ["userId1", "userId2"],
  createdAt: timestamp,
  timestamp: "ISO string"
}
```

### مجموعة `users`

معلومات المستخدمين:

```javascript
{
  displayName: "الاسم",
  email: "البريد الإلكتروني",
  photoURL: "رابط الصورة",
  isAdmin: true/false,
  age: "العمر",
  country: "البلد",
  bio: "نبذة",
  phone: "رقم الهاتف"
}
```

---

## 🔐 القواعد الأمنية

### الرسائل (messages)
- **القراءة**: الأدمن يرى كل شيء، المستخدم يرى رسائله فقط
- **الإنشاء**: أي مستخدم مسجل
- **التحديث/الحذف**: الأدمن فقط

### المستخدمون (users)
- **القراءة**: جميع المستخدمين المسجلين
- **الإنشاء**: المستخدم يمكنه إنشاء ملفه فقط
- **التحديث**: الأدمن أو المستخدم نفسه
- **الحذف**: الأدمن فقط

### المنشورات (posts)
- **القراءة**: الجميع
- **الإنشاء**: الأدمن فقط
- **التحديث**: المستخدمين المسجلين (للإعجابات والتعليقات)
- **الحذف**: الأدمن فقط

---

## 👥 الأدمن

البريدان الإلكترونيان للأدمن:
- `mchokri100@gmail.com`
- `ahmed1195@gmail.com`

يتم التحقق تلقائياً من هذه البريدات في الكود.

---

## ✅ التحقق من التفعيل

بعد نشر القواعد:

1. سجل دخول كمستخدم عادي
2. يجب أن ترى صفحة الرسائل مع الأدمن مباشرة
3. أرسل رسالة اختبار
4. سجل دخول كأدمن
5. يجب أن ترى قائمة المستخدمين
6. اختر مستخدم وشاهد رسائله

---

## 🐛 حل المشاكل

### الرسائل لا تظهر؟
- تأكد من نشر القواعد في Firebase Console
- تحقق من Console في المتصفح للأخطاء
- تأكد من تسجيل الدخول بشكل صحيح

### الأدمن لا يرى المستخدمين؟
- تأكد من أن البريد الإلكتروني للأدمن صحيح
- تحقق من مجموعة `users` في Firestore

### خطأ في الصلاحيات؟
- تأكد من نشر القواعد الجديدة
- انتظر دقيقة واحدة بعد النشر
- أعد تحميل الصفحة

---

## 📝 ملاحظات مهمة

1. ✅ النظام مبسط وفعال
2. ✅ المستخدمون يراسلون الأدمن فقط
3. ✅ الأدمن يرى جميع المستخدمين
4. ✅ الرسائل محفوظة بشكل آمن
5. ✅ التصميم متناسق مع الموقع
6. ✅ يعمل على جميع الأجهزة

---

## 🚀 الإطلاق

بعد التأكد من عمل كل شيء:

1. ✅ نشر القواعد في Firebase
2. ✅ رفع الكود على GitHub
3. ✅ Vercel سيقوم بالنشر تلقائياً
4. ✅ اختبار الموقع المباشر

---

**جاهز للاستخدام! 🎉**
