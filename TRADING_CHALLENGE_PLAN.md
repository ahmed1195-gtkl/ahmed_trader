# خطة تنفيذ خدمة التحديات التنافسية (Trading Challenge)

## نظرة عامة

إنشاء نظام تحديات تداول تنافسي مدمج في منصة Ahmed Trader، مع ربط كامل بأسواق حقيقية عبر API.

## البنية التقنية

### 1. قاعدة البيانات (Firebase Firestore)

**Collections الجديدة:**
- `challenges`: معلومات التحديات (المستويات، الشروط، الجوائز)
- `challenge_participants`: المشاركون في كل تحدي
- `challenge_trades`: الصفقات المنفذة في التحديات
- `challenge_leaderboards`: ترتيب المتداولين

### 2. مصادر البيانات الحقيقية

- **Finnhub API**: أسعار الأسهم والعملات الحقيقية
- **TwelveData API**: بيانات تاريخية وأسعار فورية
- استخدام نفس مصادر البيانات الموجودة في المشروع

### 3. المكونات الجديدة

- `TradingChallenge.jsx`: الصفحة الرئيسية للتحديات
- `ChallengeDetails.jsx`: تفاصيل تحدي معين
- `ChallengeDashboard.jsx`: لوحة تحكم المشارك
- `ChallengeAdmin.jsx`: إدارة التحديات للأدمن
- `TradingEngine.jsx`: محرك التداول الافتراضي

## الميزات الأساسية

### المرحلة 1: MVP (الإصدار الأول)

1. ثلاثة مستويات تحدي (برونزي، فضي، ذهبي)
2. نظام تداول افتراضي مرتبط بأسعار حقيقية
3. حساب تلقائي للأرباح والخسائر
4. مراقبة Drawdown في الوقت الفعلي
5. لوحة صدارة للمشاركين

### المرحلة 2: الميزات الاجتماعية

1. غرفة دردشة للمشاركين
2. عرض الصفقات المفتوحة (اختياري)
3. تحليلات مقارنة

### المرحلة 3: التحقيق الدخل

1. نظام الاشتراكات
2. الحسابات الممولة
3. إعادة المحاولة

## التكامل مع النظام الحالي

- استخدام نظام المصادقة الموجود
- التكامل مع نظام الأصدقاء
- استخدام نفس مكتبات UI (Radix, Tailwind)
- الحفاظ على نفس نمط التصميم
