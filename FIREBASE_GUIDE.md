# دليل إدارة الحسابات المعلقة (Firebase)

لقد قمت بتحديث الكود الخاص بك للتعامل مع الحسابات المعلقة. إليك كيفية تفعيل هذا المنطق من لوحة تحكم Firebase:

## 1. تعليق حساب من Firebase Authentication
إذا قمت بتعطيل مستخدم من لوحة تحكم Firebase (Disable account)، سيظهر له الخطأ التالي عند محاولة تسجيل الدخول:
> **"تم تعليق حسابك لأسباب أمنية. لا يمكنك التسجيل بنفس البريد الإلكتروني مرة أخرى."**

## 2. منع إعادة التسجيل عبر Firestore
لقد أضفت حقلاً جديداً باسم `isSuspended` في مجموعة `users` في Firestore. 
لمنع مستخدم من الدخول حتى لو كان حسابه نشطاً في Auth، يمكنك القيام بما يلي:
1. اذهب إلى **Firestore Database**.
2. ابحث عن وثيقة المستخدم (User Document) عبر الـ `uid`.
3. قم بتغيير قيمة `isSuspended` إلى `true`.

## 3. التغييرات البرمجية التي تمت
- **i18n.js**: إضافة رسائل الخطأ باللغتين العربية والإنجليزية.
- **Auth.jsx**: 
    - تحديث `translateError` للتعرف على كود الخطأ `auth/user-disabled`.
    - تحديث `checkUserOnboarding` للتحقق من حقل `isSuspended` في Firestore قبل السماح بالدخول.
    - إضافة حقل `isSuspended: false` افتراضياً عند إنشاء حساب جديد.

تم الحفاظ على كامل التصميم الحالي (الخلفية السوداء، التدرجات الذهبية، والتأثيرات الحركية) دون أي تغيير.
